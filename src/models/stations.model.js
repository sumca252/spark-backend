/**
 * Stations Model
 */

const db = require("../config/db/database.js");

// TODO: Add stations
const Stations = {
    getAllStations: () => {},
    getStationById: (id) => {},
};

module.exports = Stations;
